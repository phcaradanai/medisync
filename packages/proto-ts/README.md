# @medisync/proto

Generated TypeScript types and service descriptors from `proto/` (via `buf generate` at the repo root). Do not edit `src/gen` by hand.

Usage from an app:

```ts
import { PrescriptionCreatedSchema } from "@medisync/proto/medisync/events/v1/events_pb";
import { DispensingService } from "@medisync/proto/medisync/dispensing/v1/dispensing_pb";
```
